import React from 'react';
import './index.scss'

export default ({ type, name, placeholder, value, required, check, refs , pattern, ...rest}) => {
  return (
    <input
      className='login-regist__input'
      type={ type }
      name={ name }
      placeholder={ placeholder }
      required={required}
      value={value}
      ref={refs}
      defaultChecked={check}
      pattern={pattern}
      {...rest}
    />
  );
};

