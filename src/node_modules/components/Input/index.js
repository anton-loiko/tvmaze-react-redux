import React from 'react';
import './index.scss'

export default ({ type, name, placeholder, value, required, check, refs , autocomp, pattern, disab, ...rest}) => {
  return (
    <input
      className='login-regist__input'
      type={ type }
      name={ name }
      placeholder={ placeholder }
      required={required}
      value={value}
      ref={refs}
      defaultChecked={check}
      pattern={pattern}
      autoComplete={autocomp}
      readOnly={disab}
      {...rest}
    />
  );
};

