import React from 'react';
import { Route, Redirect } from 'react-router-dom';

export default ({ login, toregirect, render: Render, data, ...rest }) => {
  return (
    <Route
      {...rest}
      render={props => (login ? <Render data={data} {...props}/> : <Redirect to={toregirect}/>)}/>
  );
}
