const initialState = {
  search: 'girls'
};

export default (state = initialState, action) => {
  switch (action.type) {
    case 'USER_REQUEST':
      return Object.assign({}, state, {search: action.userRequest});
    default:
      return state;
  }
};
