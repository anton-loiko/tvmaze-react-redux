export default (state = {}, action) => {
  switch (action.type) {
    case 'USER_REQUEST':
      return Object.assign({}, state, {userRequest: action.userRequest});
    default:
      return state;
  }
};
