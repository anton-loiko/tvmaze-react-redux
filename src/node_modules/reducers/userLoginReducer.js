export default (state = {}, action) => {
  switch (action.type) {
    case 'USER_LOGIN':
      return Object.assign({}, state, {userLogin: action.userLogin});

    default:
      return state;
  }
};
