const initialState = {
  userLogin: {
    userName: '',
    password: ''
  }
};

export default (state = initialState, action) => {
  switch (action.type) {
    case 'USER_LOGIN':
      return Object.assign({}, state, {userLogin: action.userLogin});

    default:
      return state;
  }
};
