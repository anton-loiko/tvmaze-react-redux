import React, { Component } from 'react';
import './index.scss';
import Search from '../Search'

export default class extends Component {
  render() {
    return (
      <div>
        <Search/>
      </div>
    );
  }
}
